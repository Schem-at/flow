/**
 * Contract code generator — moved into @flow/core so flow folding can emit
 * type declarations. Re-exported here so existing client imports keep working.
 */

export { contractToTypeScript, composeBlockSource } from '@flow/core';
